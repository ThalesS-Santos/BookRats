🟢 FASE 1: Preparação do Terreno (Configuração e Aliases)
Objetivo: Garantir que o sistema entenda os novos caminhos antes de movermos os arquivos.

Etapa 1: Configuração de Path Aliases

1.1: Criar/Atualizar o arquivo jsconfig.json na raiz para definir os atalhos (ex: @ui, @core, @tests).

1.2: Configurar o babel.config.js com o plugin module-resolver para suportar os aliases no React Native.

1.3: Testar o alias em um arquivo existente para garantir que o compilador não quebra.

Etapa 2: Documentação e Ambiente

2.1: Criar a pasta raiz docs/ e mover para lá o Relatorio.txt, ideias.txt e teste automatizados.txt.

2.2: Criar o arquivo .env.example para documentar as chaves de API necessárias sem expô-las.

Etapa 2.3: Centralização de Tipos (TypeScript/JSDoc)

2.3.1: Criar src/core/types/ para definir os modelos de dados (Livro, Usuário, Badge) para que todos os módulos usem a mesma definição.

🟡 FASE 2: Reestruturação Física (Diretórios e Movimentação)
Objetivo: Organizar a "casa" separando Frontend, Backend e Testes.

Etapa 3: Isolamento do Backend e API

3.1: Criar a pasta raiz src/core/.

3.2: Mover src/services/firebase.js para src/core/firebase/.

3.3: Mover src/api/ para src/core/api/.

3.4: Atualizar os imports usando o alias @core.

Etapa 4: Centralização da UI (Frontend)

4.1: Criar a pasta raiz src/ui/.

4.2: Mover src/components/, src/screens/, src/navigation/ e src/constants/ para dentro de src/ui/.

4.3: Atualizar os imports usando o alias @ui.

Etapa 5: Consolidação de Testes e Mocks

5.1: Criar a pasta raiz tests/.

5.2: Mover src/__tests__/ para tests/suites/.

5.3: Mover src/mocks/ para tests/mocks/.

5.4: Mover jest.setup.js e jest.config.js para a pasta de configuração de testes.

Etapa 5.5: Gerenciador de Assets e Ícones

5.5.1: Criar src/ui/assets/index.js para exportar todas as imagens e ícones, permitindo imports limpos como import { Logo } from '@ui/assets'.

🟠 FASE 3: Modularização de Lógica (Zustand Slices)
Objetivo: Fatiar o "Cérebro" do app para torná-lo escalável.

Etapa 6: Slicing do Zustand (Fase 1)

6.1: Criar src/core/store/slices/.

6.2: Extrair a lógica de Autenticação do useUserStore.js para authSlice.js.

6.3: Extrair a lógica de Livros do useBookStore.js para librarySlice.js.

Etapa 7: Slicing do Zustand (Fase 2)

7.1: Extrair a lógica de Badges e Conquistas para gamificationSlice.js.

7.2: Criar o index.js (Root Store) que combina todos os slices em um único hook exportável.

Etapa 7.3: Implementação de Interceptores de API

7.3.1: Criar um apiClient.js no @core/api para injetar automaticamente tokens de autenticação e tratar erros globais (como expiração de sessão) antes que cheguem à UI.  

Etapa 7.4: Persistência de Cache Offline

7.4.1: Configurar o react-query ou uma lógica de cache no Zustand para que o app funcione instantaneamente com dados locais enquanto busca atualizações no Firebase.

🔴 FASE 4: Refinamento e Clean Code (Arquitetura Profissional)
Objetivo: Padronizar o código para nível de produção.

Etapa 8: Camada de Serviços e Hooks Customizados

8.1: Criar src/core/services/ para abrigar a lógica de negócio que hoje está misturada nas Screens.

8.2: Criar hooks de interface (ex: useGalleryLogic.js) para que as Screens fiquem apenas com o layout.

Etapa 9: Atomic Design e Padronização de UI

9.1: Reorganizar src/ui/components/ em atoms/, molecules/ e organisms/.

9.2: Auditoria de Tailwind: Mover cores e tamanhos fixos para o tailwind.config.js.

Etapa 10: Test Data Factories

10.1: Criar tests/factories/ com geradores de dados fakes (BookFactory, UserFactory).

10.2: Atualizar todas as suítes de teste para usar as factories em vez de objetos manuais.

Etapa 11: Error Boundary e Logging Profissional

11.1: Criar um componente de ErrorBoundary global para evitar que o app feche (crash) diante de um erro inesperado.  

11.2: Configurar um logger centralizado (ex: Sentry ou console estruturado) que diferencie logs de desenvolvimento de logs de produção.